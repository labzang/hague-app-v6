'use client'

/**
 * V10 Main 페이지 컴포넌트
 *
 * @remarks
 * - 아직 작업 전이므로 "메인 화면" 텍스트만 가운데 표시
 */
export default function V10Main() {
    return (
        <div className="min-h-screen bg-white text-black flex items-center justify-center">
            <h1 className="text-4xl font-bold">메인 화면</h1>
        </div>
    )
}
